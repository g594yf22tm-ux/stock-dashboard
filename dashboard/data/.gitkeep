# This directory holds the JSON cache files generated by scripts/fetch-market-data.js
# and scripts/update-analysis.js. The files are gitignored at the data/*.json level;
# this .gitkeep preserves the directory structure for fresh clones.
